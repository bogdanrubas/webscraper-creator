import React from "react";
import { LogoMenuWrapper } from "./styles";

interface LogoMenuProps { }

const LogoMenu: React.FunctionComponent<LogoMenuProps> = () => (
	<LogoMenuWrapper>
	</LogoMenuWrapper>
);

export default LogoMenu;
