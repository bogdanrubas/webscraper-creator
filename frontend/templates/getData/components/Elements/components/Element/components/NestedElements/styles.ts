import styled from "styled-components";
import { theme } from "config/theme";

export const NestedElementsWrapper = styled.div``;

export const Options = styled.div`
  margin: 15px 0 0 0;
  display: grid;
  grid-template-columns: 250px 230px;
  grid-gap: 15px;
`;
