mutation updateCrawlerVersionStatus(
    $crawlerVersionId: Int!
    $status: String!
  ) {
    updateCrawlerVersionStatus(
      crawlerVersionId: $crawlerVersionId
      status: $status
    ) {
      id
    }
  }